# Force pnpm 10 for lockfile compatibility
[phases.setup]
cmds = ["corepack enable", "corepack prepare pnpm@10.27.0 --activate"]
aptPkgs = ["ffmpeg"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm run build"]

[start]
cmd = "pnpm start"
