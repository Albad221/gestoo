{
  "name": "@gestoo/edge-functions-tests",
  "version": "0.1.0",
  "private": true,
  "description": "Tests for Gestoo Edge Functions",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.2.0",
    "vitest": "^1.2.0"
  }
}
